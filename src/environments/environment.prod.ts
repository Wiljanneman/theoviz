export const environment = {
  production: true,
  proxyUrl: '/api/claude', // Relative URL in production
  appSecret: 'REPLACE_WITH_YOUR_PRODUCTION_SECRET' // Set this to match server's APP_SECRET env var in Vercel
};
